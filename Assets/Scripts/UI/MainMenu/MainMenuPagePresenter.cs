//Tag class, MVP pattern will work out a bit differently than it should,
//namely creating a model in the presenter is not the most elegant solution,
//but I still decided to keep this solution because even this implementation
//of MVP is still better than the usual Flat solution
using UnityEngine;

public class MainMenuPagePresenter : MonoBehaviour {}